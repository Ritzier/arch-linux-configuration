[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=/usr/bin/mold"]

[build]
# target-dir = "/mnt/nvme/RustTarget"
rustc-wrapper = "sccache"

# [env]
# SCCACHE_DIR = "/mnt/nvme/sccache"

[profile.dev]
lto = false
incremental = true
debug = 1
